import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.1',
  appName: 'sampleapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
